# Git 版本管理系统 - 新手完全学习指南

## 目录
1. [什么是 Git？](#什么是-git)
2. [Git 与 SVN 的区别](#git-与-svn-的区别)
3. [核心概念](#核心概念)
4. [安装与配置](#安装与配置)
5. [基本操作](#基本操作)
6. [工作流程](#工作流程)
7. [分支管理](#分支管理)
8. [协作开发](#协作开发)
9. [常见问题与解决方案](#常见问题与解决方案)
10. [最佳实践](#最佳实践)

---

## 什么是 Git？

Git 是一个**分布式版本控制系统**，由 Linus Torvalds 于 2005 年创建，最初用于 Linux 内核开发。

### 核心特点
- **分布式**：每个开发者都有完整的代码历史副本
- **快速**：大多数操作在本地执行，速度极快
- **安全**：使用 SHA-1 哈希保证数据完整性
- **灵活**：支持多种工作流程

---

## Git 与 SVN 的区别

| 特性 | Git | SVN |
|------|-----|-----|
| 架构 | 分布式 | 集中式 |
| 速度 | 快（本地操作） | 慢（需要网络） |
| 分支 | 轻量级 | 重量级 |
| 离线工作 | 可以 | 不可以 |
| 数据完整性 | SHA-1 哈希 | 文件版本号 |

---

## 核心概念

### 1. 工作区（Working Directory）
你当前正在编辑的文件目录。

### 2. 暂存区（Staging Area / Index）
临时保存即将提交的文件变更。

### 3. 仓库（Repository）
存储所有版本历史的地方。

### 4. 提交（Commit）
一次代码快照，包含时间戳、作者、提交信息和文件快照。

### 5. 分支（Branch）
指向提交的指针，用于并行开发。

```
主分支（main/master）: A --- B --- C --- D
                         \
分支（feature）：          --- B2 --- B3
```

---

## 安装与配置

### 安装 Git

**Windows:**
- 下载：https://git-scm.com/download/win
- 安装后重启终端

**macOS:**
```bash
brew install git
```

**Linux (Ubuntu):**
```bash
sudo apt-get update
sudo apt-get install git
```

### 初始配置

```bash
# 配置用户名（全局）
git config --global user.name "你的名字"

# 配置邮箱（全局）
git config --global user.email "你的邮箱@example.com"

# 查看配置
git config --list

# 设置默认编辑器
git config --global core.editor code

# 设置自动换行（Windows）
git config --global core.autocrlf true

# 设置颜色显示
git config --global color.ui auto
```

### 配置文件位置
- 全局配置：`~/.gitconfig`（Windows: `C:\Users\用户名\.gitconfig`）
- 本地配置：`.git/config`（项目目录下）

---

## 基本操作

### 1. 创建仓库

```bash
# 初始化新仓库
git init

# 克隆远程仓库
git clone <仓库地址>
git clone https://github.com/user/repo.git
git clone https://github.com/patrickstarhfg/cqbylw.git
```

### 2. 查看状态

```bash
# 查看工作区状态
git status

# 查看详细状态
git status -s

# 查看修改内容
git diff

# 查看暂存区内容
git diff --cached

# 查看文件历史
git log <文件名>
```

### 3. 添加文件到暂存区

```bash
# 添加单个文件
git add <文件名>

# 添加所有修改的文件
git add .

# 添加所有文件（包括被删除的）
git add -A

# 添加指定模式的文件
git add *.py
git add src/*.js

# 交互式添加
git add -p
```

### 4. 提交更改

```bash
# 直接提交（跳过暂存区）
git commit -a -m "提交信息"

# 添加暂存区内容到仓库
git commit -m "提交信息"

# 修改上一次提交
git commit --amend

# 修改提交信息
git commit --amend -m "新的提交信息"

# 修改提交作者
git commit --amend --author="作者名 <email@example.com>"
```

### 5. 查看历史

```bash
# 查看提交历史
git log

# 简洁格式
git log --oneline

# 图形化显示分支
git log --graph --oneline --all

# 显示每次提交的变更
git log -p

# 显示最近 N 次提交
git log -5

# 显示特定作者的提交
git log --author="名字"

# 显示特定时间范围
git log --since="2023-01-01"
git log --until="2023-12-31"

# 统计提交信息
git shortlog -sn
```

### 6. 撤销操作

```bash
# 撤销工作区修改（未 add）
git checkout -- <文件名>
git restore <文件名>

# 撤销暂存区修改（已 add 未 commit）
git reset HEAD <文件名>
git restore --staged <文件名>

# 回到某个提交
git reset --hard <commit_id>
git reset --soft <commit_id>
git reset --mixed <commit_id>

# 撤销一次提交（创建新提交）
git revert <commit_id>
```

### 7. 查看与比较

```bash
# 查看文件内容（某个版本）
git show <commit_id>:<文件名>

# 比较工作区和暂存区
git diff

# 比较暂存区和仓库
git diff --cached

# 比较工作区和仓库
git diff HEAD

# 比较两个提交
git diff <commit1> <commit2>

# 查看文件变更历史
git blame <文件名>
```

---

## 工作流程

### 标准工作流程

```bash
# 1. 查看状态
git status

# 2. 查看修改内容
git diff

# 3. 添加文件到暂存区
git add <文件名>

# 4. 提交更改
git commit -m "描述性的提交信息"

# 5. 推送到远程仓库
git push origin main
```

### 提交信息规范

```
类型: 描述

详细说明（可选）
```

**类型：**
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具变动

**示例：**
```
feat: 添加用户登录功能
fix: 修复数据导出乱码问题
docs: 更新 README 文档
refactor: 重构数据处理模块
```

---

## 分支管理

### 什么是分支？

分支是主线开发的平行世界，允许你在不影响主代码的情况下进行开发。

### 基本分支操作

```bash
# 查看分支
git branch              # 查看本地分支
git branch -a          # 查看所有分支（包括远程）
git branch -v          # 查看分支最近提交

# 创建分支
git branch <分支名>
git branch feature/login

# 切换分支
git checkout <分支名>
git checkout feature/login

# 创建并切换分支
git checkout -b <分支名>
git checkout -b feature/login

# 合并分支
git checkout main
git merge <分支名>
git merge feature/login

# 删除分支
git branch -d <分支名>    # 安全删除（已合并）
git branch -D <分支名>    # 强制删除（未合并）
```

### 分支管理策略

#### 1. Git Flow（经典模型）

```
main/master:     ----●------●------●
                    \      \
develop:            ---●---●---●
                    /   \       \
feature:           A     B       C
```

**分支类型：**
- `main/master`: 生产环境代码
- `develop`: 开发环境代码
- `feature`: 功能开发分支
- `release`: 发布准备分支
- `hotfix`: 紧急修复分支

#### 2. GitHub Flow（简化模型）

```
main:  ---●---●---●---●
              \
feature:      ---●---●
```

**流程：**
1. `main` 分支始终可部署
2. 从 `main` 创建功能分支
3. 完成后创建 Pull Request
4. 审核通过后合并到 `main`

#### 3. Trunk Based Development（主干开发）

```
main: ---●---●---●---●---●
          \   \   \
feature:  A   B   C
```

**特点：**
- 持续集成
- 短生命周期分支
- 小步快跑

---

## 协作开发

### 远程仓库操作

```bash
# 查看远程仓库
git remote -v

# 添加远程仓库
git remote add origin <仓库地址>
git remote add upstream <上游仓库地址>

# 修改远程仓库地址
git remote set-url origin <新地址>

# 删除远程仓库
git remote remove <名称>

# 拉取远程更新
git fetch <远程名>
git fetch origin

# 拉取并合并
git pull <远程名> <分支名>
git pull origin main

# 推送到远程
git push <远程名> <分支名>
git push origin main
git push origin feature/login

# 推送并设置上游分支
git push -u origin main
```

### 同步上游仓库

```bash
# 添加上游仓库
git remote add upstream https://github.com/original/repo.git

# 从上游拉取
git fetch upstream
git checkout main
git merge upstream/main

# 推送到自己的远程
git push origin main
```

### 解决冲突

```bash
# 查看冲突文件
git status

# 手动解决冲突后
git add <冲突文件>
git commit -m "解决冲突"
```

**冲突标记：**
```
<<<<<<< HEAD
当前分支的内容
=======
其他分支的内容
>>>>>>> feature/branch
```

### 保存临时工作

```bash
# 保存当前工作
git stash
git stash save "描述信息"

# 查看 stash 列表
git stash list

# 恢复 stash
git stash pop
git stash apply

# 删除 stash
git stash drop
git stash clear
```

---

## 高级技巧

### 1. 重写历史

```bash
# 交互式 rebase
git rebase -i HEAD~3

# 选项：
# pick: 保留提交
# reword: 修改提交信息
# edit: 修改提交
# squash: 合并提交
# fixup: 合并提交（丢弃信息）
# drop: 删除提交

# 压缩最近 N 次提交
git rebase -i HEAD~5

# 将提交移动到其他分支
git cherry-pick <commit_id>
```

### 2. 标签（Tag）

```bash
# 创建轻量标签
git tag <标签名>
git tag v1.0

# 创建附注标签
git tag -a v1.0 -m "版本说明"

# 查看标签
git tag
git tag -l "v1.*"

# 推送标签
git push origin <标签名>
git push origin --tags

# 删除标签
git tag -d <标签名>
git push origin :refs/tags/<标签名>
```

### 3. 配置别名

```bash
# 设置常用别名
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.unstage 'reset HEAD'
git config --global alias.last 'log -1'
git config --global alias.visual '!gitk'

# 使用别名
git st
git co main
git br
```

### 4. 查看信息

```bash
# 查看仓库信息
git remote show origin

# 查看文件变更
git log --follow <文件名>

# 查看谁在何时修改了某行
git blame <文件名> -L 10,20

# 查看提交统计
git shortlog -sn
git log --pretty=format:'%h %s' --graph
```

---

## 常见问题与解决方案

### 1. 忘记添加文件到提交

```bash
# 修改上一次提交
git add 忘记的文件
git commit --amend
```

### 2. 提交信息写错了

```bash
git commit --amend -m "新的提交信息"
```

### 3. 推送被拒绝

```bash
# 先拉取远程更新
git pull origin main

# 解决冲突后再次推送
git push origin main
```

### 4. 撤销已推送的提交

```bash
# 创建反向提交
git revert <commit_id>
git push origin main
```

### 5. 恢复误删的文件

```bash
# 从仓库中恢复
git checkout HEAD -- <文件名>
git restore <文件名>
```

### 6. 查看某次提交的详细信息

```bash
git show <commit_id>
git show HEAD~3
```

### 7. 清理未跟踪文件

```bash
# 查看未跟踪文件
git status

# 删除未跟踪文件
git clean -f

# 删除未跟踪的目录
git clean -fd
```

---

## 最佳实践

### 1. 提交原则

- **原子性**：每次提交只做一件事
- **及时性**：频繁提交，不要积累大量更改
- **描述性**：清晰的提交信息
- **可逆性**：避免重写已推送的历史

### 2. 分支策略

- 主分支保持可部署
- 功能分支命名清晰（`feature/xxx`）
- 及时合并或同步主分支
- 删除已完成的分支

### 3. 冲突处理

- 提前同步，减少冲突
- 小步提交，降低冲突概率
- 使用工具辅助（VS Code 内置冲突解决）

### 4. 备份策略

- 定期推送到远程
- 不要只依赖本地仓库
- 重要提交打标签

### 5. 团队协作

- 统一代码风格
- 规范提交信息
- 代码审查（Pull Request）
- 文档同步更新

---

## 实用命令速查表

```bash
# 状态查看
git status          # 查看状态
git diff            # 查看修改
git log             # 查看历史

# 添加与提交
git add .           # 添加所有
git commit -m "msg" # 提交
git push            # 推送

# 分支操作
git branch          # 列出分支
git checkout -b xx  # 创建并切换
git merge xx        # 合并分支

# 回退操作
git reset --hard HEAD~1  # 回退一次
git revert <commit>      # 反向提交

# 远程操作
git fetch           # 拉取更新
git pull            # 拉取并合并
git push            # 推送
```

---

## 学习资源

### 官方文档
- Git 官网：https://git-scm.com/
- Pro Git 中文版：https://git-scm.com/book/zh/v2

### 在线工具
- Git 可视化：https://git-school.github.io/visualizing-git/
- Git 指南：https://www.git-tower.com/learn/

### 实践建议
1. 先在测试项目中练习
2. 理解概念再使用命令
3. 遇到问题查看官方文档
4. 养成使用 `git status` 的习惯

---

## 总结

Git 是软件开发的必备技能，掌握它能：
- ✅ 追踪代码变更历史
- ✅ 安全地尝试新功能
- ✅ 与团队高效协作
- ✅ 快速回滚错误更改
- ✅ 管理项目版本发布

**记住：**
- 理解概念比记忆命令更重要
- 多实践才能熟练掌握
- 不要害怕犯错（Git 可以恢复）

祝你学习愉快！🎉
